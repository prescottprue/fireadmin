export default (theme) => ({
  root: {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    marginTop: '4rem',
    width: '100%'
  },
  words: {
    marginTop: '2rem'
  }
})
